name: streamlit-refactored
channels:
  - conda-forge
dependencies:
  # 1. System & Node.js (Critical)
  - git
  - nodejs=20
  - npm

  # 2. Python Core (Install via Conda for stability)
  - python=3.10
  - pyyaml          # <--- Installed directly by Conda now
  - requests
  - streamlit
  - python-dotenv

  # 3. Pip (Only for packages NOT in Conda)
  - pip
  - pip:
    - google-genai
