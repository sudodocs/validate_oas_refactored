name: streamlit-refactored
channels:
  - conda-forge
dependencies:
  # 1. System Dependencies (The Fix)
  - git
  - nodejs=20   # Forces Node.js v20+ (Required for rdme v10)
  
  # 2. Python Setup
  - python=3.10
  - pip
  - pip:
    # 3. Your Python Libraries
    - streamlit
    - requests
    - PyYAML
    - python-dotenv
    - google-genai
