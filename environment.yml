name: streamlit-refactored
channels:
  - conda-forge
dependencies:
  # 1. System Tools (Conda handles these best)
  - git
  - nodejs=20
  - npm
  
  # 2. Python Base
  - python=3.10
  - pip
  
  # 3. Python Libraries (Move ALL of them to pip for speed)
  - pip:
    - streamlit
    - requests
    - PyYAML
    - python-dotenv
    - google-genai
